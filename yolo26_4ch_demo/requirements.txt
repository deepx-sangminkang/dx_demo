# Requirements for YOLO26 multi-channel Qt demo
# - DX-related packages (dx-engine) are assumed to be provided by the DX-RT installation
# - Only PyPI-installable packages are listed here

numpy>=1.19.5
# OpenCV is intentionally NOT pinned here. The PyPI `opencv-python` wheel is
# built WITHOUT GStreamer, which disables hardware decoding (decode: hw/auto).
# `install.sh` runs scripts/ensure_gstreamer_opencv.sh to enforce a
# GStreamer-enabled OpenCV (distro python3-opencv or a custom build).
# See README "Hardware-Accelerated Decoding".
packaging>=20.0
PySide6>=6.5.0
PyYAML>=6.0
