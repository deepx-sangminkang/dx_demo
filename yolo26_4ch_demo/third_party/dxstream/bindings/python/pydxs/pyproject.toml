[build-system]
requires = ["setuptools>=40.8.0", "wheel", "pybind11>=2.6.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pydxs"
version = "0.1.0"
description = "Python bindings for DX Stream"
requires-python = ">=3.6"
