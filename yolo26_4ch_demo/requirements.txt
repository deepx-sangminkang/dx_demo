# Requirements for YOLO26 multi-channel Qt demo
# - DX-related packages (dx-engine) are assumed to be provided by the DX-RT installation
# - Only PyPI-installable packages are listed here

numpy>=1.19.5
# NOTE: the PyPI `opencv-python` wheel is built WITHOUT GStreamer support, so
# hardware decoding (decode: hw/auto) cannot use the GStreamer backend with it.
# For HW decoding, install a GStreamer-enabled OpenCV (e.g. distro `python3-opencv`
# or a custom build). See README "Hardware-Accelerated Decoding".
opencv-python>=4.5.1
packaging>=20.0
PySide6>=6.5.0
PyYAML>=6.0
