# Requirements for the YOLOv11 multi-channel Qt demo
# - Assumes DX-related packages (dx-engine, dx-postprocess) are provided by the DX-RT / DX-APP installation
# - Only packages installable from PyPI are listed here

numpy>=1.19.5
opencv-python>=4.5.1
packaging>=20.0
PySide6>=6.5.0
PyYAML>=6.0
